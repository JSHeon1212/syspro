#include <stdio.h>

int main() {

        int n1, n2;

        printf("0 bookID: borrow book, 1 bookID: return book ) ");
        scanf("%d %d", n1, n2);

        if (n1 == 0) {
                printf("");
        }
        else if (n1 == 1) {
                printf("");
        }

        return 0;
}

