#include <stdio.h>

int main() {

        int n;

        printf("0: list of all books, 1: list of available books ) ");
        scanf("%d", n);

        if (n == 0) {
                printf("");
        }
        else if (n == 1) {
                printf("");
        }

        return 0;
}

